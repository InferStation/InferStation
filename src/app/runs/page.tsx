import ModelOverviewPage from "@/components/ModelOverviewPage";

export const metadata = {
  title: "Runs — InferStation",
};

export default function RunsPage() {
  return <ModelOverviewPage kind="runs" />;
}
