import ModelOverviewPage from "@/components/ModelOverviewPage";

export const metadata = {
  title: "Compare — InferStation",
};

export default function ComparePage() {
  return <ModelOverviewPage kind="compare" />;
}
